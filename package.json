{
  "name": "rdo-protocol-monorepo",
  "version": "1.0.0",
  "description": "RDO Protocol: Objects That Say No",
  "scripts": {
    "test:contracts": "npx hardhat test",
    "deploy:contracts": "npx hardhat run scripts/deploy.ts --network sepolia",
    "mobile": "cd rdo-mobile && npm start",
    "mobile:android": "cd rdo-mobile && npm run android",
    "mobile:ios": "cd rdo-mobile && npm run ios"
  },
  "devDependencies": {
    "hardhat": "^2.19.4",
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "typescript": "^5.0.0",
    "ts-node": "^10.9.2"
  },
  "dependencies": {
    "dotenv": "^16.4.1"
  }
}